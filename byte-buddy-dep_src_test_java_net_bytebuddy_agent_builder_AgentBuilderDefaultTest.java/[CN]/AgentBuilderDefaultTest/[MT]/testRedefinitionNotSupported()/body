{
  new AgentBuilder.Default(byteBuddy).with(initializationStrategy).with(AgentBuilder.RedefinitionStrategy.REDEFINITION).with(typeLocator).with(typeStrategy).with(installationStrategy).with(listener).with(accessControlContext).disableNativeMethodPrefix().type(typeMatcher).transform(transformer).installOn(instrumentation);
}
