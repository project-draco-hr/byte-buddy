{
  return this == other || !(other == null || getClass() != other.getClass()) && explicitInterfaces.equals(((Appender)other).explicitInterfaces) && instrumentationTarget.equals(((Appender)other).instrumentationTarget);
}
