{
  assertThat(new MethodGraph.Compiler.Default.Key<SampleKey>(FOO,Collections.singleton(foo)).hashCode(),is(new MethodGraph.Compiler.Default.Key<SampleKey>(FOO,new HashSet<SampleKey>(Arrays.asList(foo,bar))).hashCode()));
  assertThat(new MethodGraph.Compiler.Default.Key<SampleKey>(FOO,Collections.singleton(foo)),is(new MethodGraph.Compiler.Default.Key<SampleKey>(FOO,new HashSet<SampleKey>(Arrays.asList(foo,bar)))));
}
