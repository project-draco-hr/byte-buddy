{
  return this == other || !(other == null || getClass() != other.getClass()) && placeholderType.equals(((ForRebaseInstrumentation)other).placeholderType) && methodRebaseResolver.equals(((ForRebaseInstrumentation)other).methodRebaseResolver);
}
