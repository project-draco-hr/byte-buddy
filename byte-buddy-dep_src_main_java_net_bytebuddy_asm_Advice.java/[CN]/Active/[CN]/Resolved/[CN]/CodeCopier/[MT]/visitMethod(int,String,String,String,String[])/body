{
  return adviceMethod.getInternalName().equals(internalName) && adviceMethod.getDescriptor().equals(descriptor) ? new LabelSubstitutor(Active.Resolved.this.apply(methodVisitor,metaDataHandler,instrumentedMethod)) : IGNORE_METHOD;
}
