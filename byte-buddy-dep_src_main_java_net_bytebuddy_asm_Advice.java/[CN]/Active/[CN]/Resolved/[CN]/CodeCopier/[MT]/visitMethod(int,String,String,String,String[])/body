{
  return adviceMethod.getInternalName().equals(internalName) && adviceMethod.getDescriptor().equals(descriptor) ? Active.Resolved.this.apply(methodVisitor,metaDataHandler,instrumentedMethod) : IGNORE_METHOD;
}
