{
  return adviceMethod.getInternalName().equals(internalName) && adviceMethod.getDescriptor().equals(descriptor) ? new ExceptionTabelSubstitutor(Delegating.Resolved.this.apply(methodVisitor,metaDataHandler,instrumentedMethod,suppressionHandler)) : IGNORE_METHOD;
}
