{
  return adviceMethod.getDeclaredAnnotations().ofType(OnMethodExit.class).loadSilent().onThrowable() ? new WithExceptionHandler(adviceMethod,userFactories,binaryRepresentation,enterType) : new WithoutExceptionHandler(adviceMethod,userFactories,binaryRepresentation,enterType);
}
