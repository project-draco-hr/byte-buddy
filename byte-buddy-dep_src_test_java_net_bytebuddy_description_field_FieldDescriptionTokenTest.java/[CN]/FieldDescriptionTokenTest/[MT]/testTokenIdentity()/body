{
  assertThat(new FieldDescription.Token(FOO,MODIFIERS,first,Collections.singletonList(firstAnnotation)).isIdenticalTo(new FieldDescription.Token(FOO,MODIFIERS,first,Collections.singletonList(firstAnnotation))),is(true));
}
