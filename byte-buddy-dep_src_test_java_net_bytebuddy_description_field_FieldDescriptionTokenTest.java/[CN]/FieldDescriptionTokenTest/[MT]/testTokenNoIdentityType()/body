{
  assertThat(new FieldDescription.Token(FOO,MODIFIERS,first,Collections.singletonList(firstAnnotation)).isIdenticalTo(new FieldDescription.Token(FOO,MODIFIERS,second,Collections.singletonList(firstAnnotation))),is(false));
}
