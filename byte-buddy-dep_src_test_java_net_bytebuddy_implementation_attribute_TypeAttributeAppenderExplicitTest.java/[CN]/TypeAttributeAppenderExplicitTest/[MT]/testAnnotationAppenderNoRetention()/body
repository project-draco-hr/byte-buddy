{
  new TypeAttributeAppender.Explicit(new AnnotationList.ForLoadedAnnotation(new Qux.Instance())).apply(classVisitor,instrumentedType,valueFilter);
  verifyZeroInteractions(classVisitor);
  verifyZeroInteractions(instrumentedType);
}
