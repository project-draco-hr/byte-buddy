{
  TypeDescription typeDescription=mock(TypeDescription.class);
  when(typeDescription.asErasure()).thenReturn(typeDescription);
  assertThat(joinUniqueRaw(Collections.singleton(typeDescription),Collections.singleton(typeDescription)),is(Collections.singletonList(typeDescription)));
}
