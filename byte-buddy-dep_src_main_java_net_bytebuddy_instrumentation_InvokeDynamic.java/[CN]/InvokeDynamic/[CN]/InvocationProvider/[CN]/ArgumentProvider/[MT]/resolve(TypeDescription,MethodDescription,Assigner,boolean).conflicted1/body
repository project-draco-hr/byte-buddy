{
  return new Resolved.Simple(MethodVariableAccess.loadThisReferenceAndArguments(instrumentedMethod),instrumentedMethod.isStatic() ? instrumentedMethod.getParameterTypes() : join(instrumentedType,instrumentedMethod.getParameterTypes()));
}
