{
  return typeDescription.represents(void.class) ? new LookupStrategy.ForHierarchy(fieldName) : new LookupStrategy.ForExplicitType(fieldName,typeDescription.represents(TargetType.class) ? instrumentedType : typeDescription);
}
