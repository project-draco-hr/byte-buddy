{
  return typeDescription.represents(void.class) ? new LookupEngine.ForHierarchy(fieldName) : new LookupEngine.ForExplicitType(fieldName,typeDescription.equals(TargetType.DESCRIPTION) ? instrumentedType : typeDescription);
}
