{
  assertThat(new FieldRegistry.Default().include(latentFieldMatcher,distinctFactory,null).prepare(instrumentedType).compile(fallback).target(knownField).getFieldAppender(),is(distinct));
  assertThat(new FieldRegistry.Default().include(latentFieldMatcher,distinctFactory,null).prepare(instrumentedType).compile(fallback).target(unknownField),is(fallback));
}
