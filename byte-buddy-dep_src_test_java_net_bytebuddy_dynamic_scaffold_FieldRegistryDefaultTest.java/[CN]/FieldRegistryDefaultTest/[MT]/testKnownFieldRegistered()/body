{
  assertThat(new FieldRegistry.Default().include(latentFieldMatcher,distinct,null).compile(instrumentedType,fallback).target(knownField).getFieldAppenderFactory(),is(distinct));
  assertThat(new FieldRegistry.Default().include(latentFieldMatcher,distinct,null).compile(instrumentedType,fallback).target(unknownField),is(fallback));
}
