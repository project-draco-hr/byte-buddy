{
  assertThat(ArrayFactory.forType(TypeDescription.Generic.OBJECT).new ArrayStackManipulation(Collections.<StackManipulation>singletonList(StackManipulation.Illegal.INSTANCE)).isValid(),is(false));
}
