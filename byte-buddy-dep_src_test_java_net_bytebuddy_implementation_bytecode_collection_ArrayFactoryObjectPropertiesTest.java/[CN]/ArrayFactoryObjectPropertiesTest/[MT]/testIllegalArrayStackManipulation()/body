{
  assertThat(ArrayFactory.forType(new TypeDescription.ForLoadedType(Object.class)).new ArrayStackManipulation(Collections.<StackManipulation>singletonList(StackManipulation.Illegal.INSTANCE)).isValid(),is(false));
}
