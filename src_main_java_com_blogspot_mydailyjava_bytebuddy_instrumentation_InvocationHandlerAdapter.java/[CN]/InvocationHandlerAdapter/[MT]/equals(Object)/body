{
  return this == other || !(other == null || getClass() != other.getClass()) && fieldName.equals(((InvocationHandlerAdapter)other).fieldName) && invocationHandler.equals(((InvocationHandlerAdapter)other).invocationHandler);
}
