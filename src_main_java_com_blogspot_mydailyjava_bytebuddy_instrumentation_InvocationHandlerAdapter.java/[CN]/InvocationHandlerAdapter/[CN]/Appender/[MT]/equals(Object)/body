{
  return this == other || !(other == null || getClass() != other.getClass()) && instrumentedType.equals(((Appender)other).instrumentedType) && InvocationHandlerAdapter.this.equals(((Appender)other).getJavaProxyAdapter());
}
