{
  assertThat(new SubclassInstrumentationTarget.Factory(bridgeMethodResolverFactory,SubclassInstrumentationTarget.OriginTypeIdentifier.SUPER_TYPE).make(finding,methodList).getOriginType(),is(superType));
}
