{
  return this == other || !(other == null || getClass() != other.getClass()) && classLoader.equals(((ClassLoaderParentMatcher)other).classLoader) && accessControlContext.equals(((ClassLoaderParentMatcher)other).accessControlContext);
}
