{
  return new ByteBuddy().withIgnoredMethods(none()).subclass(BASE_CLASS).method(isDeclaredBy(ExampleClass.class)).intercept(SuperMethodCall.INSTANCE).make().load(newClassLoader(),ClassLoadingStrategy.Default.INJECTION).getLoaded().newInstance();
}
