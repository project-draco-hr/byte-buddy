{
  assertThat(new FieldRegistry.Default().include(latentFieldMatcher,distinct).compile(instrumentedType,fallback).target(knownField),is(distinct));
  assertThat(new FieldRegistry.Default().include(latentFieldMatcher,distinct).compile(instrumentedType,fallback).target(unknownField),is(fallback));
}
