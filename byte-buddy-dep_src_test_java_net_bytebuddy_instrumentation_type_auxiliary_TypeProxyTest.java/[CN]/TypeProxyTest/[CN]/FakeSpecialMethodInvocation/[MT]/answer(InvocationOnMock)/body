{
  MethodDescription accessedMethod=(MethodDescription)invocation.getArguments()[0];
  return new Container(instrumentedType.getDeclaredMethods().filter(isMethod().and(nameEndsWithIgnoreCase(accessedMethod.getName()))).getOnly());
}
