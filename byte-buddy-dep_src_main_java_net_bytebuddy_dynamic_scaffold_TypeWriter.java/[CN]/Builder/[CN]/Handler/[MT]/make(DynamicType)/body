{
  classVisitor.visitEnd();
  return new DynamicType.Default.Unloaded<S>(instrumentedType,classWriter.toByteArray(),loadedTypeInitializer,join(instrumentationContext.getRegisteredAuxiliaryTypes(),Arrays.asList(dynamicType)));
}
