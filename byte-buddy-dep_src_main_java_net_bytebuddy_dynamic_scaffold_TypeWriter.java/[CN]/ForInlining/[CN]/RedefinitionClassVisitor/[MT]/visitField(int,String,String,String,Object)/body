{
  FieldDescription fieldDescription=declaredFields.remove(internalName);
  return fieldDescription == null ? super.visitField(modifiers,internalName,descriptor,genericSignature,defaultValue) : redefine(fieldDescription,defaultValue);
}
