{
  stringBuilder.append('.');
  stringBuilder.append(ownerType.getSort().isParameterized() ? typeDescription.getName().substring(ownerType.asErasure().getName().length() + 1) : typeDescription.getName());
}
