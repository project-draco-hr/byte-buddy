{
  TypeDescription componentType=asErasure().getComponentType();
  return componentType == null ? UNDEFINED : new OfNonGenericType.Latent(componentType,Collections.<AnnotationDescription>emptyList());
}
