{
  TypeList.Generic lowerBounds=wildcard.getLowerBounds();
  return lowerBounds.isEmpty() ? wildcard.getUpperBounds().getOnly().accept(this) : new Dispatcher.ForContravariantType(lowerBounds.getOnly());
}
