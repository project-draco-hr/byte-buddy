{
  TypeList.Generic lowerBounds=wildcard.getLowerBounds();
  return lowerBounds.isEmpty() ? OfWildcardType.Latent.boundedAbove(wildcard.getUpperBounds().getOnly().accept(this)) : OfWildcardType.Latent.boundedBelow(lowerBounds.getOnly().accept(this));
}
