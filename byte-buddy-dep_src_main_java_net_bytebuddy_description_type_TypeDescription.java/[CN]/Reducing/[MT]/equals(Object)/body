{
  return this == other || !(other == null || getClass() != other.getClass()) && declaringType.equals(((Reducing)other).declaringType) && typeVariableTokens.equals(((Reducing)other).typeVariableTokens);
}
