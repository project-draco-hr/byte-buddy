{
  return this == other || !(other == null || getClass() != other.getClass()) && super.equals(other) && rawType.equals(((OfParameterizedType)other).rawType)&& ownerType.equals(((OfParameterizedType)other).ownerType)&& parameterTypes.equals(((OfParameterizedType)other).parameterTypes);
}
