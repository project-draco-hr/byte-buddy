{
  return ownerType == null && parameterTypes.isEmpty() ? new Generic.OfNonGenericType.Latent(rawType,annotations) : new Generic.OfParameterizedType.Latent(rawType,ownerType,parameterTypes,annotations);
}
