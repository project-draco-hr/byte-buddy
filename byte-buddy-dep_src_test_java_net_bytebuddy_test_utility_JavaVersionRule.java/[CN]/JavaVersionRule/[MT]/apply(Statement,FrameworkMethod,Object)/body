{
  Enforce enforce=method.getAnnotation(Enforce.class);
  return enforce == null || enforce.type().matches(ClassFileVersion.forKnownJavaVersion(enforce.value()).compareTo(supportedVersion)) ? base : new NoOpStatement(enforce.value(),enforce.type());
}
