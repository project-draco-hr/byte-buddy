{
  return Arrays.asList(new Object[][]{{SynchronizationState.is(false),0},{SynchronizationState.PLAIN,0},{SynchronizationState.is(true),Opcodes.ACC_SYNCHRONIZED},{SynchronizationState.SYNCHRONIZED,Opcodes.ACC_SYNCHRONIZED}});
}
