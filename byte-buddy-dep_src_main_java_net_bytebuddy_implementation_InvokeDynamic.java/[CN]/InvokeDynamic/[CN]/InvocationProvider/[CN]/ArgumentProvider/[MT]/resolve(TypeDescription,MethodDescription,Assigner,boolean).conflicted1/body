{
  return new Resolved.Simple(MethodVariableAccess.allArgumentsOf(instrumentedMethod.asDefined()).prependThisReference(),instrumentedMethod.isStatic() ? instrumentedMethod.getParameters().asTypeList().asRawTypes() : join(instrumentedMethod.getDeclaringType().asRawType(),instrumentedMethod.getParameters().asTypeList().asRawTypes()));
}
