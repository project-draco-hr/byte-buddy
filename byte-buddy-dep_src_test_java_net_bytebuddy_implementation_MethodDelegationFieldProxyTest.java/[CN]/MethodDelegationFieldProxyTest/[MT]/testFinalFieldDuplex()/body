{
  DynamicType.Loaded<FinalField> loaded=implement(FinalField.class,MethodDelegation.to(SwapDuplex.class).appendParameterBinder(FieldProxy.Binder.install(GetSet.class)));
  loaded.getLoaded().getDeclaredConstructor().newInstance().method();
}
