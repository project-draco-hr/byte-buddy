{
  return new MethodDelegation(instrumentationDelegate,parameterBinders,defaultsProvider,ambiguityResolver,assigner,isNotEmpty(methodList.filter(nonNull(methodMatcher)),NO_METHODS_ERROR_MESSAGE));
}
