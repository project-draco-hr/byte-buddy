{
  return materialize(new MethodToken(methodToken.getInternalName(),methodToken.getReturnType(),methodToken.getParameterTypes(),uniqueTypes(Arrays.asList(nonNull(type))),methodToken.getModifiers()));
}
