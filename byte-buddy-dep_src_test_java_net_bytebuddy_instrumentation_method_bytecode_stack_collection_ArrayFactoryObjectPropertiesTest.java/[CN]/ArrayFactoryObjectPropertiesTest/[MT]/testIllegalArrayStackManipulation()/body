{
  assertThat(ArrayFactory.targeting(new TypeDescription.ForLoadedType(Object.class)).new ArrayStackManipulation(Arrays.<StackManipulation>asList(StackManipulation.Illegal.INSTANCE)).isValid(),is(false));
}
