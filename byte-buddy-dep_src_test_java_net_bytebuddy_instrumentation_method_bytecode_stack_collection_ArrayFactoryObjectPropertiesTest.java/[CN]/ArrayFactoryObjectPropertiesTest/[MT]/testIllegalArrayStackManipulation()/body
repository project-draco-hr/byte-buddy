{
  assertThat(ArrayFactory.targeting(new TypeDescription.ForLoadedType(Object.class)).new ArrayStackManipulation(Collections.singletonList(StackManipulation.Illegal.INSTANCE)).isValid(),is(false));
}
