{
  assertThat(new AgentBuilder.RawMatcher.ForElementMatcherPair(typeMatcher,classLoaderMatcher).matches(typeDescription,classLoader,Object.class,protectionDomain),is(false));
  verify(classLoaderMatcher).matches(classLoader);
  verifyNoMoreInteractions(classLoaderMatcher);
  verifyZeroInteractions(typeMatcher);
}
