{
  TypePool typePool=mock(TypePool.class);
  TypeDescription typeDescription=mock(TypeDescription.class);
  when(typePool.describe(FOO)).thenReturn(typeDescription);
  assertThat(new TypePool.LazyTypeDescription.DeclarationContext.DeclaredInType(FOO_INTERNAL).getEnclosingType(typePool),is(typeDescription));
}
