{
  new ByteBuddy().redefine(Box.class).visit(new AsmVisitorWrapper.ForDeclaredMethods().writerFlags(ClassWriter.COMPUTE_FRAMES).method(named(BAR),Advice.to(IllegalParameterSubstitutionAdvice.class))).make();
}
