{
  Class<?> type=new ByteBuddy().redefine(FrameSample.class).visit(Advice.to(FrameAdviceWithSuppression.class).on(named(BAR)).writerFlags(ClassWriter.COMPUTE_FRAMES)).make().load(ClassLoadingStrategy.BOOTSTRAP_LOADER,ClassLoadingStrategy.Default.WRAPPER).getLoaded();
  assertThat(type.getDeclaredMethod(BAR,String.class).invoke(null,FOO),is((Object)FOO));
  assertThat(type.getField(COUNT).getInt(null),is((Object)2));
}
