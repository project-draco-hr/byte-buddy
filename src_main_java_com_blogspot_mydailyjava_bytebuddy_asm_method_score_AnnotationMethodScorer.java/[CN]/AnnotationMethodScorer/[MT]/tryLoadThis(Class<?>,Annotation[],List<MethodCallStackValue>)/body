{
  if (tryFind(This.class,targetMethodArgumentAnnotation) != null && matches(targetMethodArgumentType,sourceTypeName)) {
    targetMethodCallStack.add(new ThisReference());
    return true;
  }
  return false;
}
