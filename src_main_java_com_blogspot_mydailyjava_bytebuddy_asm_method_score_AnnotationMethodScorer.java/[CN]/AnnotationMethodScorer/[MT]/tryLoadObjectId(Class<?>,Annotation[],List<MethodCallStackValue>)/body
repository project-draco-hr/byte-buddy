{
  if (tryFind(ObjectId.class,targetMethodArgumentAnnotation) != null && isUuid(targetMethodArgumentType)) {
    targetMethodCallStack.add(new ObjectIdReference());
    return true;
  }
  return false;
}
