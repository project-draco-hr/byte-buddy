{
  return this == other || !(other == null || getClass() != other.getClass()) && cacheMethods == ((InvocationHandlerAdapter)other).cacheMethods && fieldName.equals(((InvocationHandlerAdapter)other).fieldName);
}
