{
  GenericTypeDescription typeVariable=substitute.getDeclaredMethods().filter(is(methodDescription)).getOnly().findVariable(original.getSymbol());
  if (typeVariable == null) {
    throw new IllegalStateException("Cannot resolve type variable " + original.getSymbol() + " for "+ methodDescription);
  }
  return typeVariable;
}
