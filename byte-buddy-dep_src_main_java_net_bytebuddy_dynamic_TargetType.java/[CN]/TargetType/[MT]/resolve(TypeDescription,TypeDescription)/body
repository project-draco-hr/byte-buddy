{
  int arity=0;
  while (typeDescription.isArray()) {
    typeDescription=typeDescription.getComponentType();
    arity++;
  }
  return typeDescription.represents(TargetType.class) ? TypeDescription.ArrayProjection.of(actualTargetType,arity) : typeDescription;
}
