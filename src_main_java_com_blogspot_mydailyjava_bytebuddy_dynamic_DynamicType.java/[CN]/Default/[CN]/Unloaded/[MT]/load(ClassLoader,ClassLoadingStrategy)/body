{
  Map<String,byte[]> types=getHelperTypesRaw();
  types.put(getMainTypeName(),getMainTypeByte());
  return new Default.Loaded<T>(mainTypeName,mainTypeByte,typeInitializers,helperTypes,initialize(classLoadingStrategy.load(classLoader,types)));
}
