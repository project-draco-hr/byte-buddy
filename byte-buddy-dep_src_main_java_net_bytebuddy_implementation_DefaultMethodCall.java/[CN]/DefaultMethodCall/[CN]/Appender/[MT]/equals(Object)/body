{
  return this == other || !(other == null || getClass() != other.getClass()) && prioritizedInterfaces.equals(((Appender)other).prioritizedInterfaces) && implementationTarget.equals(((Appender)other).implementationTarget);
}
