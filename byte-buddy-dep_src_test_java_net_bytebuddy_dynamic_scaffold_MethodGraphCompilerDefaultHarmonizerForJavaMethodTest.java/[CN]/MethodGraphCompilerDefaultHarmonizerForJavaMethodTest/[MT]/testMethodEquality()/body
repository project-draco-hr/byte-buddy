{
  assertThat(harmonizer.harmonize(new MethodDescription.TypeToken(first,Collections.singletonList(first))),is(harmonizer.harmonize(new MethodDescription.TypeToken(first,Collections.singletonList(first)))));
}
