{
  assertThat(harmonizer.wrap(new MethodDescription.TypeToken(first,Collections.singletonList(first))),is(harmonizer.wrap(new MethodDescription.TypeToken(first,Collections.singletonList(first)))));
}
