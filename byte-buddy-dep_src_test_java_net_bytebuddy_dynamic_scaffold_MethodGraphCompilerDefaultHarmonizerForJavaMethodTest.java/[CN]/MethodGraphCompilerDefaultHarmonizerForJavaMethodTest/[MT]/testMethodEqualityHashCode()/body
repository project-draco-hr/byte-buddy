{
  assertThat(harmonizer.wrap(new MethodDescription.TypeToken(first,Collections.singletonList(first))).hashCode(),is(harmonizer.wrap(new MethodDescription.TypeToken(first,Collections.singletonList(first))).hashCode()));
}
