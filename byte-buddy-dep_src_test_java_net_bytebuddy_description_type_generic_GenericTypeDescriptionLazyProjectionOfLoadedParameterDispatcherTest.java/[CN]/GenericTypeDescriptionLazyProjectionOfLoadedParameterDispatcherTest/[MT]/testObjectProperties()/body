{
  final Iterator<Method> methods=Arrays.asList(Object.class.getDeclaredMethods()).iterator();
  ObjectPropertyAssertion.of(GenericTypeDescription.LazyProjection.OfLoadedParameter.Dispatcher.ForModernVm.class).create(new ObjectPropertyAssertion.Creator<Method>(){
    @Override public Method create(){
      return methods.next();
    }
  }
).apply();
  ObjectPropertyAssertion.of(GenericTypeDescription.LazyProjection.OfLoadedParameter.Dispatcher.ForLegacyVm.class).apply();
}
