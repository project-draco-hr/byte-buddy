{
  doReturn(annotationType).when(argumentBinder).getHandledType();
  AnnotationDrivenBinder.ArgumentBinder.IdentifiedBinding<?> identifiedBinding=mock(AnnotationDrivenBinder.ArgumentBinder.IdentifiedBinding.class);
  when(identifiedBinding.isValid()).thenReturn(bindingResult);
  when(identifiedBinding.getIdentificationToken()).thenReturn(identificationToken);
  when(identifiedBinding.getStackManipulation()).thenReturn(bindingResult ? LegalTrivialStackManipulation.INSTANCE : IllegalStackManipulation.INSTANCE);
  when(((AnnotationDrivenBinder.ArgumentBinder)argumentBinder).bind(any(Annotation.class),anyInt(),any(MethodDescription.class),any(MethodDescription.class),any(TypeDescription.class),any(Assigner.class))).thenReturn(identifiedBinding);
  return identifiedBinding;
}
