{
  assertThat(harmonizer.wrap(new MethodDescription.TypeToken(first,Collections.singletonList(first))),not(harmonizer.wrap(new MethodDescription.TypeToken(second,Collections.singletonList(first)))));
}
