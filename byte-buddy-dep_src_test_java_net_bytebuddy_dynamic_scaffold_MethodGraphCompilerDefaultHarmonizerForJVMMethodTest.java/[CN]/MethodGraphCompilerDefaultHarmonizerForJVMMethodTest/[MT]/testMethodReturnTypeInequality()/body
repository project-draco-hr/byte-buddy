{
  assertThat(harmonizer.harmonize(new MethodDescription.TypeToken(first,Collections.singletonList(first))),not(harmonizer.harmonize(new MethodDescription.TypeToken(second,Collections.singletonList(first)))));
}
