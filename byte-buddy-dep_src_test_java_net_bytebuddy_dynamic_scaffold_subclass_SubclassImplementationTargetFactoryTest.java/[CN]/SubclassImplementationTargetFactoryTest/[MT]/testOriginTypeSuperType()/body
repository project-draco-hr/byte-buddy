{
  assertThat(new SubclassImplementationTarget.Factory(bridgeMethodResolverFactory,SubclassImplementationTarget.OriginTypeIdentifier.SUPER_TYPE).make(finding,methodList).getOriginType(),is(superType));
}
