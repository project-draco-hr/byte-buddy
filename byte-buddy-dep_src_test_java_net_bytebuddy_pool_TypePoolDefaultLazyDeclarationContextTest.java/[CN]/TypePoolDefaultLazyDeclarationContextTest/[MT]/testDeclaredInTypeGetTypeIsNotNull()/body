{
  TypePool typePool=mock(TypePool.class);
  TypeDescription typeDescription=mock(TypeDescription.class);
  when(typePool.describe(FOO)).thenReturn(new TypePool.Resolution.Simple(typeDescription));
  assertThat(new TypePool.Default.LazyTypeDescription.DeclarationContext.DeclaredInType(FOO_INTERNAL).getEnclosingType(typePool),is(typeDescription));
}
