{
  Class<?> annotationType=Class.forName(TypeDescription.ForLoadedType.getName(annotation.annotationType()),false,classLoader);
  return annotationType == annotation.annotationType() ? annotation : AnnotationInvocationHandler.of(classLoader,(Class<S>)annotationType,asValue(annotation));
}
