{
  AnnotationDescription.Loadable<Target> target=getAnnotationType().getDeclaredAnnotations().ofType(Target.class);
  return EnumSet.copyOf(Arrays.asList(target == null ? DEFAULT_TARGET : target.loadSilent().value()));
}
