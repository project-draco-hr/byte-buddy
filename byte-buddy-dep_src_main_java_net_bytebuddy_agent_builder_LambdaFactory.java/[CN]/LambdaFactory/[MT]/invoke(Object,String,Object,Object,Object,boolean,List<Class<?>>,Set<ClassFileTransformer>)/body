{
  try {
    return (byte[])dispatcher.invoke(target,caller,invokedName,invokedType,samMethodType,implMethod,enforceSerialization,markerInterfaces,classFileTransformers);
  }
 catch (  Exception exception) {
    throw new IllegalStateException(exception);
  }
}
