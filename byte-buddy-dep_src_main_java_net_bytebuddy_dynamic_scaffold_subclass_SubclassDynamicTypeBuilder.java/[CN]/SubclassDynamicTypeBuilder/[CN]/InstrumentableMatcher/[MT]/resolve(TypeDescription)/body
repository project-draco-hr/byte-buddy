{
  return (ElementMatcher<? super MethodDescription>)isVirtual().and(not(isFinal())).and(isVisibleTo(instrumentedType)).and(not(ignoredMethods.resolve(instrumentedType))).or(isDeclaredBy(instrumentedType));
}
