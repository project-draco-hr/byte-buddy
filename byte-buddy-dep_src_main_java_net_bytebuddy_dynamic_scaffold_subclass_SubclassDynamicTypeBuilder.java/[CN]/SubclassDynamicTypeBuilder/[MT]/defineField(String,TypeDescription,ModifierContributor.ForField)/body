{
  FieldToken fieldToken=new FieldToken(isValidIdentifier(name),nonNull(fieldType),resolveModifierContributors(FIELD_MODIFIER_MASK,nonNull(modifier)));
  return new SubclassFieldValueTarget<T>(fieldToken,defaultFieldAttributeAppenderFactory);
}
