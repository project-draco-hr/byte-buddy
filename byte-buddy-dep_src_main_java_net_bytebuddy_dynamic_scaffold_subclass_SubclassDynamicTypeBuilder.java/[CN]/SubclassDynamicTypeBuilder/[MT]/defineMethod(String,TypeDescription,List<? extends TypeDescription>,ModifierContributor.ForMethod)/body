{
  return new SubclassExceptionDeclarableMethodInterception<T>(new MethodToken(isValidIdentifier(name),nonNull(returnType),nonNull(parameterTypes),Collections.<TypeDescription>emptyList(),resolveModifierContributors(METHOD_MODIFIER_MASK,nonNull(modifier))));
}
