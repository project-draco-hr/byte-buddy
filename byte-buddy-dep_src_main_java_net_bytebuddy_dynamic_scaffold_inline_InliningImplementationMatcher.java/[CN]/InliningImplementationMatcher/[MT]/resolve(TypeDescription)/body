{
  return (ElementMatcher<? super MethodDescription>)not(ignoredMethods).and(isOverridable().or(isDeclaredBy(instrumentedType))).or(isDeclaredBy(instrumentedType).and(not(predefinedMethodSignatures)));
}
