{
  if (this == other)   return true;
  if (other == null || getClass() != other.getClass())   return false;
  Entry entry=(Entry)other;
  return compiledHandler.equals(entry.compiledHandler) && attributeAppender.equals(entry.attributeAppender) && bridges.equals(entry.bridges);
}
