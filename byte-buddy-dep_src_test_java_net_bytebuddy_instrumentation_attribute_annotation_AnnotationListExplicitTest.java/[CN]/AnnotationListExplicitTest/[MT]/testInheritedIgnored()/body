{
  AnnotationList annotationList=new AnnotationList.Explicit(Arrays.asList(foo,bar)).inherited(Collections.singleton(new TypeDescription.ForLoadedType(Bar.class)));
  assertThat(annotationList.size(),is(0));
}
