{
  assertDynamicType(typeWriter.members().writeFields(Arrays.asList(firstField,secondField),compiledFieldRegistry).writeMethods(Arrays.asList(simpleMethod,fromAbstractMethod,toAbstractMethod,skippedMethod),compiledMethodRegistry).make(),true,true);
}
