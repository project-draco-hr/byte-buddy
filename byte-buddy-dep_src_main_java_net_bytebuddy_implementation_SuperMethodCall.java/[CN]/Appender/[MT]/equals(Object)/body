{
  return this == other || !(other == null || getClass() != other.getClass()) && implementationTarget.equals(((Appender)other).implementationTarget) && terminationHandler.equals(((Appender)other).terminationHandler);
}
