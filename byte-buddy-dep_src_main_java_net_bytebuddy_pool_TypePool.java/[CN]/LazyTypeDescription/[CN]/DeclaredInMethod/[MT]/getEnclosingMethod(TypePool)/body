{
  return getEnclosingType(typePool).getDeclaredMethods().filter(MethodDescription.CONSTRUCTOR_INTERNAL_NAME.equals(methodName) ? isConstructor() : named(methodName).and(hasMethodDescriptor(methodDescriptor))).getOnly();
}
