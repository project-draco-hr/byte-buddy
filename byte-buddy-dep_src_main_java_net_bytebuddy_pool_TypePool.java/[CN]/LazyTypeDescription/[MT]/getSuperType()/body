{
  return superTypeDescriptor == null || isInterface() ? TypeDescription.Generic.UNDEFINED : signatureResolution.resolveSuperType(superTypeDescriptor,typePool,superTypeAnnotationTokens.get(-1),this);
}
