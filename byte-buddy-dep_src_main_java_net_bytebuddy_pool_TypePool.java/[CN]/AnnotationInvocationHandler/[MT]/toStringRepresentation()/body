{
  StringBuilder toString=new StringBuilder();
  toString.append('@');
  toString.append(annotationType.getName());
  toString.append('(');
  boolean firstMember=true;
  for (  Map.Entry<Method,AnnotationValue<?,?>> entry : values.entrySet()) {
    if (firstMember) {
      firstMember=false;
    }
 else {
      toString.append(", ");
    }
    toString.append(entry.getKey().getName());
    toString.append('=');
    toString.append(PropertyDispatcher.of(entry.getKey().getReturnType()).toString(entry.getValue().load(classLoader,entry.getKey())));
  }
  toString.append(')');
  return toString.toString();
}
