{
  Map<String,List<AnnotationToken>> annotationTokens=this.annotationTokens.get(index);
  return boundTypeTokens.get(index).toGenericType(typePool,typeVariableSource,EMPTY_TYPE_PATH,annotationTokens == null ? Collections.emptyMap() : annotationTokens);
}
