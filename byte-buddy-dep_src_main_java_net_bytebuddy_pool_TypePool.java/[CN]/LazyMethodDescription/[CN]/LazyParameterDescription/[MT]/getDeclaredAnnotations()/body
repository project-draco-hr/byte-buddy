{
  List<AnnotationToken> annotationTokens=parameterAnnotationTokens.get(index);
  return annotationTokens == null ? new AnnotationList.Empty() : LazyAnnotationDescription.asList(typePool,annotationTokens);
}
