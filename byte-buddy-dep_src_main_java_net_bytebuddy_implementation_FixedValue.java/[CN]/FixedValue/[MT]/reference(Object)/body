{
  if (fixedValue == null) {
    return new ForPoolValue(NullConstant.INSTANCE,TypeDescription.OBJECT,defaultAssigner(),true);
  }
  return new ForStaticField(fixedValue,defaultAssigner(),defaultConsiderRuntimeType());
}
