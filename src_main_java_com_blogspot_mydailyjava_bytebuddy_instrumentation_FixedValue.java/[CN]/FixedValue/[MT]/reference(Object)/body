{
  if (fixedValue == null) {
    return new ForPoolValue(NullConstant.INSTANCE,Object.class,defaultAssigner(),true);
  }
  return new ForStaticField(fixedValue,defaultAssigner(),defaultConsiderRuntimeType());
}
