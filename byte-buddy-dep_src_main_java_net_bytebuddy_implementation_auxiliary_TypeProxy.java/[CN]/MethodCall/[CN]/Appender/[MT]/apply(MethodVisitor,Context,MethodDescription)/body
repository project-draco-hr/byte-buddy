{
  StackManipulation.Size stackSize=implement(instrumentedMethod,invocationFactory.invoke(implementationTarget,proxiedType,instrumentedMethod)).apply(methodVisitor,instrumentationContext);
  return new Size(stackSize.getMaximalSize(),instrumentedMethod.getStackSize());
}
