{
  if (this == other)   return true;
  if (other == null || getClass() != other.getClass())   return false;
  ArrayFactory that=(ArrayFactory)other;
  return arrayCreator.equals(that.arrayCreator) && componentType.equals(that.componentType) && sizeDecrease.equals(that.sizeDecrease);
}
