{
  MethodDescription accessorMethod=registeredGetters.get(fieldDescription);
  if (accessorMethod == null) {
    accessorMethod=new MethodDescription.Latent(instrumentedType,String.format("%s$%s$%s",fieldDescription.getName(),ACCESSOR_METHOD_SUFFIX,randomString.nextString()),fieldDescription.getType().asRawType(),Collections.<ParameterDescription.Token>emptyList(),resolveModifier(fieldDescription.isStatic()),Collections.<GenericTypeDescription>emptyList(),Collections.<AnnotationDescription>emptyList());
    registerGetter(fieldDescription,accessorMethod);
  }
  return accessorMethod;
}
