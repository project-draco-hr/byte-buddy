{
  return methodDescription.isTypeInitializer() ? isTypeInitializer() : (methodDescription.isConstructor() ? isConstructor() : named(methodDescription.getName())).and(takesArguments(methodDescription.getParameterTypes()));
}
