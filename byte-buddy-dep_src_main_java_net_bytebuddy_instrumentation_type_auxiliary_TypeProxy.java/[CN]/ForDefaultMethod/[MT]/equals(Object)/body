{
  if (this == other)   return true;
  if (other == null || getClass() != other.getClass())   return false;
  ForDefaultMethod that=(ForDefaultMethod)other;
  return serializableProxy == that.serializableProxy && instrumentationTarget.equals(that.instrumentationTarget) && proxiedType.equals(that.proxiedType);
}
