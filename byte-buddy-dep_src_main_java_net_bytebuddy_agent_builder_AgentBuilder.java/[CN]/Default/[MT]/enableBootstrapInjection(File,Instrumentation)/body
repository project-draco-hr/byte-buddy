{
  return new Default(byteBuddy,binaryLocator,listener,nativeMethodPrefix,accessControlContext,true,retransformation,new BootstrapInjectionStrategy.Enabled(nonNull(folder),nonNull(instrumentation)),entries);
}
