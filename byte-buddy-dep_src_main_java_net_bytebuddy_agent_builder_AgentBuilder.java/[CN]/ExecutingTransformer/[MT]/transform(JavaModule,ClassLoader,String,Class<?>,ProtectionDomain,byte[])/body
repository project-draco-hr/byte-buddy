{
  if (internalTypeName == null) {
    return NO_TRANSFORMATION;
  }
  return AccessController.doPrivileged(new ExecutionDispatcher(module,classLoader,internalTypeName.replace('/','.'),classBeingRedefined,protectionDomain,binaryRepresentation),accessControlContext);
}
