{
  return new Default(byteBuddy,binaryLocator,listener,nativeMethodPrefix,disableSelfInitialization,retransformation,bootstrapInjectionStrategy,join(new Transformation(rawMatcher,transformer),entries));
}
