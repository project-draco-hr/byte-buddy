{
  return new Default(byteBuddy,binaryLocator,listener,nativeMethodPrefix,accessControlContext,disableSelfInitialization,retransformation,bootstrapInjectionStrategy,join(new Transformation(rawMatcher,transformer),entries));
}
