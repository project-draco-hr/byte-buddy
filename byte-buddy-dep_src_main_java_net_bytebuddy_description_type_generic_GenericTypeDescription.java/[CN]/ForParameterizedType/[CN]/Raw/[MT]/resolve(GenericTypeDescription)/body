{
  return typeDescription != null && typeDescription.getSort().isNonGeneric() && !typeDescription.asRawType().getTypeVariables().isEmpty() ? new Raw(typeDescription.asRawType()) : typeDescription;
}
