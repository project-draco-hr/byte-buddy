{
  for (  Entry entry : entries) {
    if (entry.matches(fieldDescription)) {
      return entry.bind(fieldDescription);
    }
  }
  return new Record.ForSimpleField(fieldDescription,AnnotationAppender.ValueFilter.Default.APPEND_DEFAULTS);
}
