{
  return new DefaultExceptionDeclarableMethodInterception(new MethodToken(nonNull(parameterTypes),Collections.<TypeDescription>emptyList(),resolveModifierContributors(METHOD_MODIFIER_MASK & ~Opcodes.ACC_STATIC,nonNull(modifier))));
}
