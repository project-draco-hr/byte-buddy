{
  if (!methodDescription.isMethod()) {
    throw new IllegalArgumentException("Not a method: " + methodDescription);
  }
  return defineMethod(methodDescription.getName(),methodDescription.getReturnType().asRawType(),methodDescription.getParameters().asTypeList().asRawTypes(),methodDescription.getModifiers());
}
