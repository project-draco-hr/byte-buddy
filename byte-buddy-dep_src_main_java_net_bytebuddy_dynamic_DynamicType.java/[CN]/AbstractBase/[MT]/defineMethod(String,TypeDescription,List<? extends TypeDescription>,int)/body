{
  return new DefaultExceptionDeclarableMethodInterception(new MethodToken(isValidIdentifier(name),isActualTypeOrVoid(returnType),isActualType(parameterTypes),Collections.<TypeDescription>emptyList(),modifiers));
}
