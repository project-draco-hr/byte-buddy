{
  return new RebaseInstrumentationTarget(finding,bridgeMethodResolverFactory,new MethodFlatteningResolver.Default(finding.getTypeDescription().getDeclaredMethods(),ignoredMethods,placeholderType));
}
