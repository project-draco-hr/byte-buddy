{
  return this == other || !(other == null || getClass() != other.getClass()) && super.equals(other) && methodRedefinitionResolver.equals(((RebaseInstrumentationTarget)other).methodRedefinitionResolver);
}
