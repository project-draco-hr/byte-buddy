{
  if (targetMethodArgumentAnnotations.get(InstanceId.class) != null && isUuidType(targetMethodArgumentType)) {
    targetMethodCallStack.add(new InstanceIdReference());
    return true;
  }
  return false;
}
