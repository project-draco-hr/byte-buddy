{
  if (targetMethodArgumentAnnotations.get(This.class) != null && typeCompatibilityEvaluator.isAssignable(targetMethodArgumentType,classTypeName)) {
    targetMethodCallStack.add(new ThisReference());
    return true;
  }
  return false;
}
